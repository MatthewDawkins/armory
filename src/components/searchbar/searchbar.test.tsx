import React from "react";
import { render, shallow } from "enzyme";
import { Searchbar } from "./searchbar";

describe("Searchbar", () => {

  it("shallow renders", () => {
    shallow(<Searchbar />)
  });






});
